# Environment Configuration for Plugin Optimizations

# Email Service (Resend)
RESEND_API_KEY=your_resend_api_key_here
RESEND_FROM=Little Harvest <noreply@littleharvest.co.za>

# Redis Cache (Cloud Redis)
REDIS_URL="redis://default:lPlUesUIbgfoJg1uMsoQlQSMboq83qeG@redis-16875.c341.af-south-1-1.ec2.redns.redis-cloud.com:16875"

# Rate Limiting (Upstash)
# Uses the same Redis instance as caching

# Logging (Pino)
LOG_LEVEL=info
# Options: error, warn, info, debug

# Metrics (Prometheus)
PROMETHEUS_ENABLED=true
METRICS_ENDPOINT=/api/metrics

# Grafana Configuration (Optional)
GRAFANA_URL=http://localhost:3001
GRAFANA_API_KEY=your_grafana_api_key

# Existing configurations (keep these)
NEXTAUTH_SECRET=your_nextauth_secret
NEXTAUTH_URL=http://localhost:3000
DATABASE_URL=your_database_url
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email fallback (Nodemailer - can be removed after migration)
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your_email@gmail.com
EMAIL_SERVER_PASSWORD=your_app_password
EMAIL_FROM=noreply@littleharvest.co.za
